;;
;; LiteFx renderer modules bindings to fennel
;; The main purpose is to have good name conventions (kebab-case instead of snake_case)
;; Links: https://www.cliki.net/Naming+conventions
;;

(set renderer.begin-frame renderer.begin_frame)
(set renderer.end-frame renderer.end_frame)
